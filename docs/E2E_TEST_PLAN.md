# mike-product-calc E2E 测试计划

> 版本：v0.2.0 | 更新：2026-04-11 | 负责人：Eric

---

## 1. 测试范围

**被测系统**：mike-product-calc（Python + Streamlit Web UI）  
**测试数据**：蜜可诗产品库.xlsx（真实业务数据）  
**验收标准**：所有 E2E 场景可正常执行，Oracle 命令全绿

---

## 2. 环境准备

```bash
cd /Users/ericmr/Documents/GitHub/mike-product-calc

# 1. 虚拟环境
python -m venv .venv && source .venv/bin/activate
pip install -U pip -r requirements.txt

# 2. 验证依赖
python -m py_compile src/mike_product_calc/**/*.py
python -m py_compile app.py

# 3. 启动服务（后台）
streamlit run app.py --server.headless true --server.port 8501 &

# 4. 确认端口
lsof -i :8501 | grep LISTEN
```

---

## 3. 单元/集成测试（自动化）

```bash
# Oracle 全量验证
python -m py_compile src/mike_product_calc/**/*.py && echo "COMPILE OK"

pytest tests/ -v --tb=short
```

**通过标准**：0 failures

---

## 4. E2E 功能测试用例

### TC-001：Excel 上传与解析

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 访问 http://localhost:8501 | 显示上传区 |
| 2 | 上传蜜可诗产品库.xlsx | 文件解析完成，进度消失 |
| 3 | 观察概览 tab | Sheet 数 = 13，Issues > 0（有数据校验问题预期内）|
| 4 | 下载 data_validation_report.csv | 文件正常下载，内容非空 |

**Oracle**：`python3 -m py_compile` 无报错，CSV 非空

---

### TC-002：SKU 毛利分析（Tab2）

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 切换到「SKU 毛利分析」tab | 显示毛利表 |
| 2 | 口径切换：出厂 → 门店 | 数值变化，列标题变化 |
| 3 | 状态过滤：上线 → 下线 | 行数/内容变化 |
| 4 | 点击任意 SKU 成本瀑布 | 柱状图渲染，table 有数据 |
| 5 | 调整目标毛利率 slider | 建议成本表实时更新 |

**Oracle**：毛利率 delta < 0.5pp（F-002 oracle 验证）

---

### TC-003：Sheet 浏览（Tab3）

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 切换 tab | 默认选中第一个 sheet |
| 2 | 下拉选择「产品毛利表_Gelato」| DataFrame 渲染，Rows>0 |
| 3 | 下拉切换到「产品出品表_Gelato」| 数据正常切换 |

---

### TC-004：原料价格模拟器（Tab4）

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 新建版本「测试调价」 | 版本创建成功 |
| 2 | 添加 1 个原料调价 | 新价格写入 |
| 3 | 保存版本 | 版本出现在列表 |
| 4 | 再建一版本，对比 | 差异表出现，高风险标红 |

**Oracle**：高风险 SKU 数量 ≥ 0（可预期 ≥ 1）

---

### TC-005：生产计划录入（Tab5）

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 新建场景「春季备货」 | 场景创建 |
| 2 | data_editor 添加 3 行数据 | 数据可编辑，行数正确 |
| 3 | 下载 CSV 模板 | CSV 正常下载 |
| 4 | 上传已填充的 CSV | 数据回填成功 |
| 5 | 保存场景 | 保存成功，切换场景正常 |

**Oracle**：`python -m py_compile app.py` 无报错

---

### TC-006：备料计划引擎（Tab6）

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 选择 TC-005 创建的场景 | 场景加载 |
| 2 | 设置日期范围 2026-04-01~2026-04-30 | 日期筛选生效 |
| 3 | 点击「展开 BOM」| 展开 <3s，原料需求表出现 |
| 4 | 切换到「缺口预警」tab | 显示缺口数量（预期 ≥ 1）|
| 5 | 切换到「统计概览」tab | 4个 metric 显示 |

**Oracle**：耗时 < 3s（BOM 展开性能）

---

### TC-007：采购建议页（Tab7）

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 选择 TC-005 场景 | 场景加载 |
| 2 | 点击「生成采购建议」| 建议表出现 |
| 3 | 开启「仅显示紧急项」| 紧急项过滤生效 |
| 4 | 下载采购建议 CSV | CSV 正常下载 |

---

### TC-008：产品组合评估（Tab8）

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | multiselect 选 3 个 SKU | 数量 input 出现 |
| 2 | 填入数量，切换口径 | 6 个 KPI 卡片实时更新 |
| 3 | 保存方案 A/B/C | 三个方案全部保存 |
| 4 | 查看已保存方案概览 | expander 展开显示明细 |
| 5 | 运行对比（≥2方案）| 对比表 + diff 表出现 |

---

### TC-009：多场景对比（Tab9）

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 新建 2 个场景，各选不同 SKU | 场景管理正常 |
| 2 | 点击「对比场景」| 对比汇总出现 |
| 3 | 查看 KPI 行（最优利润/毛利率）| 数值正确显示 |
| 4 | 查看 SKU 变动明细 | 差异行高亮 |

---

### TC-010：选品优化器（Tab10）

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 预选 8 个 SKU 进入候选池 | 池列表正常 |
| 2 | 设置约束：产能 200 / 预算 50000 / 最低 1 件 | 约束生效 |
| 3 | 点击「运行优化」| Top-3 方案出现 |
| 4 | 查看推荐原因文字 | 文字非空 |
| 5 | 下载 Top-3 CSV | CSV 正常下载 |

**Oracle**：枚举完成，Top-3 有结果

---

### TC-011：产能需求估算（Tab11）

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 选择 TC-005 场景 | 场景加载 |
| 2 | 视图切「按 SKU」→ 点击分析 | 柱状图 + 明细表出现 |
| 3 | 高压力项（≥60分）红色标注 | 红色项存在（预期 ≥ 1）|
| 4 | 视图切「按日期」→ 重新分析 | 日期聚合视图正常 |

---

## 5. 性能测试

| 测试项 | 方法 | 通过标准 |
|--------|------|---------|
| BOM 展开 <3s | Tab6 计时 | elapsed < 3.0s |
| 毛利实时计算 | Tab8 计时 | elapsed < 2.0s |
| 枚举优化（8 SKU / qty≤20）| Tab10 计时 | elapsed < 10.0s |
| Python 启动冷加载 | `time python -c "import mike_product_calc"` | < 5.0s |

---

## 6. 数据边界测试

| 测试项 | 输入 | 预期 |
|--------|------|------|
| 空 Sheet 上传 | 空白 xlsx | 显示 0 sheet，正常提示 |
| 无出品表的 SKU | 任意 SKU | Tab6 展开为空，不报错 |
| 零成本产品 | 出品表 cost=0 | 毛利计算不崩溃，margin=None |
| 负值单价 | 出品表 price<0 | 识别为 error，校验报告体现 |

---

## 7. 回归清单

每次代码变更后执行：

```bash
# 1. 编译检查
python -m py_compile src/mike_product_calc/**/*.py
python -m py_compile app.py

# 2. 单元测试
pytest tests/ -q

# 3. E2E 冒烟（TC-001 必做）
# TC-001 + TC-002 + TC-005 + TC-006 + TC-008 五个核心场景
```

**通过标准**：所有命令 exit 0，无 ERROR/FAIL

---

## 8. 缺陷记录模板

```
ID:          TC-XXX
标题:        ...
环境:        macOS + Chrome / Streamlit 8501
步骤:        1. ... 2. ...
预期:        ...
实际:        ...
严重性:      P0/P1/P2
截图:        [粘贴]
```
